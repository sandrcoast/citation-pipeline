# Citation Pipeline — Python dependencies
# Install: pip install -r requirements.txt
#
# Verified against imports:
#   fastapi, pydantic       → middleware/proxy.py, core/models.py
#   uvicorn                 → runtime (uvicorn middleware.proxy:app)
#   aiohttp                 → core/extractor.py, middleware/proxy.py
#   chromadb                → storage/store.py
#   pytest                  → tests/test_pipeline.py

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Async HTTP client (Ollama communication)
aiohttp>=3.10.0

# Vector storage (embedded, zero-config)
chromadb>=0.5.0

# Testing
pytest>=8.3.0
